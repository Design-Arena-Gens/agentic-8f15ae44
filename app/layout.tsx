import type { Metadata } from "next";
import { Playfair_Display, Inter } from "next/font/google";
import "./globals.css";

const playfair = Playfair_Display({
  subsets: ["latin"],
  variable: "--font-playfair",
  display: "swap",
});

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Switch it Up Gallery | Luxury Skincare, Made Simple",
  description: "Switch on the Beauty in You with our luxurious range of skincare products. From facial cleansers to glowing oils, unveil your radiance.",
  keywords: "skincare, luxury skincare, facial cleanser, lightening cream, shower gel, glowing oil, beauty products",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${playfair.variable} ${inter.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
